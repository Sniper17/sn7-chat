# SN7 Chat V1
Chat particular responsivo.
- !rank -> API Central /kick/ranking
- !wake
- !health
- preparado para !bf, !classe e !meta.
Não ligamos as três últimas ainda porque precisamos das rotas exatas que seus comandos atuais usam.
Render: Build `pip install -r requirements.txt`; Start `gunicorn app:app`.
